type: object
properties:
  academicSystems:
    type: array
    items:
      $ref: './AcademicSystem.yaml'

  acceptedTeacherQualifications:
    type: array
    items:
      $ref: './AcceptedTeacherQualification.yaml'

  countries:
    type: array
    items:
      $ref: '../regional/countries/CountrySet2.yaml'

  subjects:
    type: array
    items:
      $ref: './Subject.yaml'

