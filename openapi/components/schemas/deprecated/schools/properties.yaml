# wrong data type
approvedByTh:
  type: integer
  format: int32
  example: 1

# wrong data type
visitedByTh:
  type: integer
  format: int32
  example: 1

# not directly related to school
country:
  type: object
  properties:
    id:
      type: integer
      format: int32
      example: 48
    name:
      type: string
      example: Morocco

# not directly related to school
region:
  type: object
  properties:
    id:
      type: integer
      format: int32
      example: 1
    name:
      type: string
      example: Africa
