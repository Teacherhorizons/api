Response_regional-countryArticles:
  oneOf:
    - $ref: "#/Response_regional-countryArticles_admin"
    - $ref: "#/Response_regional-countryArticles_school"
    - $ref: "#/Response_regional-countryArticles_teacher"
  discriminator:
    propertyName: schema
    mapping:
      admin: "#/Response_regional-countryArticles_admin"
      school: "#/Response_regional-countryArticles_school"
      teacher: "#/Response_regional-countryArticles_teacher"

Response_regional-countryArticles_admin:
  type: object
  additionalProperties: false
  properties:
    schema:
      type: string
      enum:
        - admin
    data:
      type: object
      additionalProperties: false
      properties:
        type:
          $ref: "../../models/json-api/regional/countryArticle.yaml#/JsonApiModel_regional-countryArticle_type"
        id:
          $ref: "../../models/json-api/regional/countryArticle.yaml#/JsonApiModel_regional-countryArticle_id"
        attributes:
          type: object
          additionalProperties: false
          properties:
            #simple attributes
            title:
              $ref: "../../models/library/regional/countryArticle.yaml#/regional-countryArticle_title"
            snippet:
              $ref: "../../models/library/regional/countryArticle.yaml#/regional-countryArticle_snippet"
            url:
              $ref: "../../models/library/regional/countryArticle.yaml#/regional-countryArticle_url"
            photoUrl:
              $ref: "../../models/library/regional/countryArticle.yaml#/regional-countryArticle_photoUrl"
            photoDescription:
              $ref: "../../models/library/regional/countryArticle.yaml#/regional-countryArticle_photoDescription"
            #relationship attribute
            category:
              $ref: "../../models/library/regional/countryArticle.yaml#/regional-countryArticle_category"
      required:
        - type
        - id
        - attributes
        - relationships
  required:
    - schema
    - data

Response_regional-countryArticles_school:
  type: object
  additionalProperties: false
  properties:
    schema:
      type: string
      enum:
        - school
    data:
      type: object
      additionalProperties: false
      properties:
        type:
          $ref: "../../models/json-api/regional/countryArticle.yaml#/JsonApiModel_regional-countryArticle_type"
        id:
          $ref: "../../models/json-api/regional/countryArticle.yaml#/JsonApiModel_regional-countryArticle_id"
        attributes:
          type: object
          additionalProperties: false
          properties:
            #simple attributes
            title:
              $ref: "../../models/library/regional/countryArticle.yaml#/regional-countryArticle_title"
            snippet:
              $ref: "../../models/library/regional/countryArticle.yaml#/regional-countryArticle_snippet"
            url:
              $ref: "../../models/library/regional/countryArticle.yaml#/regional-countryArticle_url"
            photoUrl:
              $ref: "../../models/library/regional/countryArticle.yaml#/regional-countryArticle_photoUrl"
            photoDescription:
              $ref: "../../models/library/regional/countryArticle.yaml#/regional-countryArticle_photoDescription"
            #relationship attribute
            category:
              $ref: "../../models/library/regional/countryArticle.yaml#/regional-countryArticle_category"
      required:
        - type
        - id
        - attributes
        - relationships
  required:
    - schema
    - data

Response_regional-countryArticles_teacher:
  type: object
  additionalProperties: false
  properties:
    schema:
      type: string
      enum:
        - teacher
    data:
      type: object
      additionalProperties: false
      properties:
        type:
          $ref: "../../models/json-api/regional/countryArticle.yaml#/JsonApiModel_regional-countryArticle_type"
        id:
          $ref: "../../models/json-api/regional/countryArticle.yaml#/JsonApiModel_regional-countryArticle_id"
        attributes:
          type: object
          additionalProperties: false
          properties:
            #simple attributes
            title:
              $ref: "../../models/library/regional/countryArticle.yaml#/regional-countryArticle_title"
            snippet:
              $ref: "../../models/library/regional/countryArticle.yaml#/regional-countryArticle_snippet"
            url:
              $ref: "../../models/library/regional/countryArticle.yaml#/regional-countryArticle_url"
            photoUrl:
              $ref: "../../models/library/regional/countryArticle.yaml#/regional-countryArticle_photoUrl"
            photoDescription:
              $ref: "../../models/library/regional/countryArticle.yaml#/regional-countryArticle_photoDescription"
            #relationship attribute
            category:
              $ref: "../../models/library/regional/countryArticle.yaml#/regional-countryArticle_category"
      required:
        - type
        - id
        - attributes
        - relationships
  required:
    - schema
    - data

