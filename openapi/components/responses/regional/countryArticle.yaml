Response_regional-countryArticles:
  oneOf:
    - $ref: "#/Response_regional-countryArticles_notSignedIn"
  discriminator:
    propertyName: schema
    mapping:
      notSignedIn: "#/Response_regional-countryArticles_notSignedIn"

Response_regional-countryArticles_notSignedIn:
  type: object
  additionalProperties: false
  properties:
    schema:
      type: string
      enum:
        - not-signed-in
    data:
      type: array
      items:
        type: object
        additionalProperties: false
        properties:
          type:
            $ref: "../../models/json-api/regional/countryArticle.yaml#/JsonApiModel_regional-countryArticle_type"
          id:
            $ref: "../../models/json-api/regional/countryArticle.yaml#/JsonApiModel_regional-countryArticle_id"
          attributes:
            type: object
            additionalProperties: false
            properties:
              #simple attributes
              title:
                $ref: "../../models/library/regional/countryArticle.yaml#/regional-countryArticle_title"
              snippet:
                $ref: "../../models/library/regional/countryArticle.yaml#/regional-countryArticle_snippet"
              url:
                $ref: "../../models/library/regional/countryArticle.yaml#/regional-countryArticle_url"
              photoUrl:
                $ref: "../../models/library/regional/countryArticle.yaml#/regional-countryArticle_photoUrl"
              photoDescription:
                $ref: "../../models/library/regional/countryArticle.yaml#/regional-countryArticle_photoDescription"
              #relationship attribute
              category:
                $ref: "../../models/library/regional/countryArticle.yaml#/regional-countryArticle_category"
        required:
          - type
          - id
          - attributes
          - relationships
  required:
    - schema
    - data
