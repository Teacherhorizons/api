JsonApiModel_application-status:
  type: object
  properties:
    schema:
      type: string
      example: admin
  discriminator:
    propertyName: schema
    mapping:
      admin: "#/JsonApiModel_application-status_admin"

JsonApiModel_application-status_admin:
  allOf:
    - $ref: "#/JsonApiModel_application-status"
    - type: object
    - properties:
        $ref: "#/JsonApiModel_application-status_attributes_admin"
    - required:
        - type
        - id

# admin
JsonApiModel_application-status_attributes_admin:
  type:
    $ref: "#/JsonApiModel_application-status_type"
  id:
    $ref: "#/JsonApiModel_application-status_id"
  attributes:
    allOf:
      - $ref: "../../library/application/status.yaml#/application-status_standardProperties_adminOnly"

# shared
JsonApiModel_application-status_type:
  type: string
  example: application-status
  description: application-status
  readOnly: true

JsonApiModel_application-status_id:
  type: string
  example: "1"
  readOnly: true
  description: Unique identifier of the application-status (string)
