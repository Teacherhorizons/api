# yaml-language-server: $schema=../../../../../meta/schemas/None.json

JsonApiModel_success_204:
  description: NO_CONTENT (Request successful)
  content:
    application/json:
      schema:
        type: object
        nullable: true
        default: null
