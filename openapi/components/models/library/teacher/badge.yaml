# simple attributes
teacher-badge_name:
  type: string
  example: Guest blogger

teacher-badge_precedence:
  type: integer
  example: Foo bar

teacher-badge_slug:
  type: string
  example: guest-blogger

teacher-badge_isAutomated:
  type: boolean
  example: true
