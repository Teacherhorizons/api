#attributes
school-acceptedTeacherQualification_creationDate:
  type: string
  format: date-time
  example: 2011-02-07T15:53:34Z

school-acceptedTeacherQualification_latestUpdateDate:
  type: string
  format: date-time
  example: 2011-02-07T15:53:34Z

# relationship attributes
school-acceptedTeacherQualification_schoolId:
  type: integer
  example: 1
  description: Relationship to School

school-acceptedTeacherQualification_creationUserId:
  type: integer
  example: 1
  description: Relationship to User

school-acceptedTeacherQualification_latestUpdateUserId:
  type: integer
  example: 1
  description: Relationship to User

school-acceptedTeacherQualification_teacherQualificationTypeId:
  type: integer
  example: 1
  description: Relationship to teacher

# relationships
school-acceptedTeacherQualification_school:
  type: object
  additionalProperties: false
  properties:
    id:
      type: string
      example: "1"
      description: Relationship to school
  required: 
    - id

school-acceptedTeacherQualification_teacherQualificationType:
  type: object
  additionalProperties: false
  properties:
    id:
      type: string
      example: "1"
      description: "Relationship to teacher"
  required: 
    - id

school-acceptedTeacherQualification_creationUser:
  type: object
  additionalProperties: false
  properties:
    id:
      type: string
      example: "1"
      description: Relationship to user
  required: 
    - id

school-acceptedTeacherQualification_latestUpdateUser:
  type: object
  additionalProperties: false
  properties:
    id:
      type: string
      example: "1"
      description: Relationship to user
  required: 
    - id
  
