# standard properties
general-educationLevel_standardProperties_all:
  allOf:
    - $ref: "#/general-educationLevel_standardProperties_shared"
    - $ref: "#/general-educationLevel_standardProperties_adminOnly"

general-educationLevel_standardProperties_shared:
  properties:
    id:
      type: integer
      example: 1
    name:
      type: string
      example: PhD

general-educationLevel_standardProperties_adminOnly:
  properties:
    id:
      type: integer
      example: 1
    name:
      type: string
      example: PhD

# relationship properties
