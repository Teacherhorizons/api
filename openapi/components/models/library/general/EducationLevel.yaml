# standard properties
educationLevel_standardProperties_all:
  allOf:
    - $ref: "#/educationLevel_standardProperties_shared"
    - $ref: "#/educationLevel_standardProperties_adminOnly"

educationLevel_standardProperties_shared:
  properties:
    id:
      type: integer
      example: 1
    name:
      type: string
      example: PhD

educationLevel_standardProperties_adminOnly:
  properties:
    id:
      type: integer
      example: 1
    name:
      type: string
      example: PhD

# relationship properties
