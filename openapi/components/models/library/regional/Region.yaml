# standard properties
regional_region_standardProperties_all:
  allOf:
    - $ref: "#/regional_region_standardProperties_shared"
    - $ref: "#/regional_region_standardProperties_adminOnly"

regional_region_standardProperties_shared:
  properties:
    alias:
      type: string
      example: /Africa
    name:
      type: string
      example: Africa
    slug:
      type: string
      example: africa
    locationLatLong:
      type: object
      properties:
        lat:
          type: number
          example: 12.2755988905617
          description: Latitude
        long:
          type: number
          example: 17.46826171875
          description: Longitude

regional_region_standardProperties_adminOnly:
  properties:
    creationDate:
      type: string
      format: date-time
      example: 2011-02-07T15:53:31Z
      description: admin only
    latestUpdateDate:
      type: string
      format: date-time
      example: 2011-02-07T15:53:31Z
      description: admin only

# relationship properties

regional_region_relationshipProperties_all:
  allOf:
    # - $ref: "#/regional_region_standardProperties_shared"
    - $ref: "#/regional_region_standardProperties_adminOnly"

# regional_region_relationshipProperties_shared:

regional_region_relationshipProperties_adminOnly:
  properties:
    creationUserId:
      type: integer
      example: 1
      description: Relationship to User; admin only
    latestUpdateUserId:
      type: integer
      example: 1
      description: Relationship to User; admin only
