#attributes
regional-regionPhoto_precedence:
  type: integer
  description: Main
  example: 1

#relationship attributes
regional-regionPhoto_regionId:
  type: integer
  example: 1
  description: Relationship to regional-region
regional-regionPhoto_photoId:
  type: integer
  example: 1
  description: Relationship to general-photo

#relationships
regional-regionPhoto_region:
  type: object
  additionalProperties: false
  properties:
    id:
      type: string
      example: "1"
      description: Relationship to regional-region
  required:
    - id
  
regional-regionPhoto_photo:
  type: object
  additionalProperties: false
  properties:
    id:
      type: string
      example: "1"
      description: Relationship to general-photo
  required:
    - id
