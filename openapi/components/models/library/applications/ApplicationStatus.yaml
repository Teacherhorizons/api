# standard properties
application-status_standardProperties_all:
  allOf:
    - $ref: "#/application-status_standardProperties_shared"
    - $ref: "#/application-status_standardProperties_adminOnly"

application-status_standardProperties_shared:
  properties:
    id:
      type: integer
      example: 1
    name:
      type: string
      example: Open

application-status_standardProperties_adminOnly:
  allOf:
    - $ref: "#/application-status_standardProperties_shared"
