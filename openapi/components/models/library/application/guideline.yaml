# simple attributes
application-guideline_name:
  type: string
  description: <span class="website"></span> Main
  example: "'Submission to Adviser' should not come directly after 'Submission to Adviser'"
  maxLength: 255

# relationship attributes
application-guideline_fromEventTypeId:
  type: integer
  nullable: true
  example: 1
  description: |
   <span class="website"></span> Relationship to [application-eventType](/#tag/simple-model-application-eventType)

application-guideline_toEventTypeId:
  type: integer
  nullable: true
  example: 1
  description: |
    <span class="website"></span> Relationship to [application-eventType](/#tag/simple-model-application-eventType)
