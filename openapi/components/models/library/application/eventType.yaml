# attributes
application-eventType_name:
  type: string
  description: Main
  example: Info request from Candidate

application-eventType_nameForSchool:
  type: string
  example: Info request from Candidate

application-eventType_nameForTeacher:
  type: string
  example: Info request from Candidate

application-eventType_isVisibleToSchool:
  type: boolean
  example: true

application-eventType_isVisibleToTeacher:
  type: boolean
  example: true

# relationship attributes
application-eventType_applicationStatusId:
  type: integer
  example: 1
  description: |
    Relationship to [application-status](/#tag/simple-model-application-status)
