# # attributes
# application-eventType_attributes:
#   allOf:
#     - $ref: "#/application-eventType_properties_name"

# relationshipIds
application-eventType_relationshipIds:
  allOf:
    - $ref: "#/application-eventType_properties_applicationStatusId"

# attribute relationships

# relationships
application-eventType_relationships:
  allOf:
    - $ref: "#/application-eventType_properties_applicationStatus"

#properties
application-eventType_properties_id:
  properties:
    id:
      $ref: "#/application-eventType_id"

application-eventType_properties_name:
  properties:
    name:
      $ref: "#/application-eventType_name"

application-eventType_properties_applicationStatusId:
  properties:
    applicationStatusId:
      $ref: "#/application-eventType_applicationStatusId"

application-eventType_properties_applicationStatus:
  properties:
    applicationStatus:
      $ref: "#/application-eventType_applicationStatus"

# attributes
application-eventType_id:
  type: integer
  example: 1

application-eventType_name:
  type: string
  example: Info request from Candidate

application-eventType_nameForSchool:
  type: string
  example: Info request from Candidate

application-eventType_nameForTeacher:
  type: string
  example: Info request from Candidate

application-eventType_isVisibleToSchool:
  type: boolean
  example: true

# relationship attributes
application-eventType_applicationStatusId:
  type: integer
  example: 1
  description: Relationship to application_status

# relationships
application-eventType_applicationStatus:
  type: object
  properties:
    id:
      type: string
      example: "1"
      description: Relationship to application_status


      
