# TODO: add non-restrictive schema

SimpleModel_school-thRating:
  type: object
  additionalProperties: false
  properties:
    # simple attributes
    value:
      $ref: "../../library/school/thRating.yaml#/school-thRating_value"
  required:
    - id
