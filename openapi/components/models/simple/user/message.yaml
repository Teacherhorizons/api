SimpleModel_user-message:
  allOf:
    # - $ref: "#/SimpleModel_user-message"
    - type: object
      allOf:
        - $ref: "#/SimpleModel_user-message_id"
        - $ref: "../../library/user/message.yaml#/user-message_attributes"
        - $ref: "../../library/user/message.yaml#/user-message_relationshipIds"

# shared
SimpleModel_user-message_id:
  properties:
    id:
      type: integer
      format: int32
      example: 1
      readOnly: true
      description: Unique identifier of the object
