SimpleModel_user:
  allOf:
    # - $ref: "#/SimpleModel_user"
    - type: object
      allOf:
        - $ref: "#/SimpleModel_user_id"
        - $ref: "../../library/user/user.yaml#/user_attributes"

# shared
SimpleModel_user_id:
  properties:
    id:
      type: integer
      format: int32
      example: 1
      readOnly: true
      description: Unique identifier of the object
