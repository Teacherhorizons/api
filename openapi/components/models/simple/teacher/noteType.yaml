# TODO AS: rename to teacher-noteType and add more attributes / relationships

SimpleModel_teacher-noteType:
  allOf:
    # - $ref: "#/SimpleModel_teacher-noteType"
    - type: object
      allOf:
        - $ref: "#/SimpleModel_teacher-noteType_type"
        - $ref: "#/SimpleModel_teacher-noteType_id"
        - $ref: "../../library/teacher/noteType.yaml#/teacher-noteType_attributes"

# shared

SimpleModel_teacher-noteType_type:
  properties:
    type:
      type: string
      example: teacher-noteType
      description: teacher-noteType
      readOnly: true

SimpleModel_teacher-noteType_id:
  properties:
    id:
      type: integer
      format: int32
      example: 1
      readOnly: true
      description: Unique identifier of the object
