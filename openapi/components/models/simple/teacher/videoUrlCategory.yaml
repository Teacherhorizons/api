SimpleModel_teacher-videoUrlCategory:
  allOf:
    - $ref: "#/SimpleModel_teacher-videoUrlCategory"
    - type: object
      allOf:
        - $ref: "#/SimpleModel_teacher-videoUrlCategory_type"
        - $ref: "#/SimpleModel_teacher-videoUrlCategory_id"
        - $ref: "../../library/teacher/videoUrlCategory.yaml#/teacher-videoUrlCategory_attributes"

# shared

SimpleModel_teacher-videoUrlCategory_type:
  properties:
    type:
      type: string
      example: teacher-videoUrlCategory
      description: teacher-videoUrlCategory
      readOnly: true

SimpleModel_teacher-videoUrlCategory_id:
  properties:
    id:
      type: integer
      format: int32
      example: 1
      readOnly: true
      description: Unique identifier of the object

