SimpleModel_teacher-qualificationType:
  allOf:
    # - $ref: "#/SimpleModel_teacher-qualificationType"
    - type: object
      allOf:
        - $ref: "#/SimpleModel_teacher-qualificationType_id"
        - $ref: "../../library/teacher/qualificationType.yaml#/teacher-qualificationType_attributes"
        - $ref: "../../library/teacher/qualificationType.yaml#/teacher-qualificationType_relationships"

# shared
SimpleModel_teacher-qualificationType_id:
  properties:
    id:
      type: integer
      format: int32
      example: 1
      readOnly: true
      description: Unique identifier of the object
