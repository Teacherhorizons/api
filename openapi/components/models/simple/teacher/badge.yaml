SimpleModel_teacher-badge:
  allOf:
    - $ref: "#/SimpleModel_teacher-badge"
    - type: object
      allOf:
        - $ref: "#/SimpleModel_teacher-badge_type"
        - $ref: "#/SimpleModel_teacher-badge_id"
        - $ref: "../../library/teacher/badge.yaml#/teacher-badge_attributes"

# shared

SimpleModel_teacher-badge_type:
  properties:
    type:
      type: string
      example: teacher-badge
      description: teacher-badge
      readOnly: true

SimpleModel_teacher-badge_id:
  properties:
    id:
      type: integer
      format: int32
      example: 1
      readOnly: true
      description: Unique identifier of the object

