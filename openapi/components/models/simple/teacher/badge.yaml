SimpleModel_teacher-badge:
  type: object
  additionalProperties: false
  properties:
    id: 
      $ref: "#/SimpleModel_teacher-badge_id"
    # simple attributes
    name:
      $ref: "../../library/teacher/badge.yaml#/teacher-badge_name"
    precedence:
      $ref: "../../library/teacher/badge.yaml#/teacher-badge_precedence"
    slug:
      $ref: "../../library/teacher/badge.yaml#/teacher-badge_slug"
    isAutomated:
      $ref: "../../library/teacher/badge.yaml#/teacher-badge_isAutomated"
  required:
    - id

# shared

# SimpleModel_teacher-badge_type:
#   properties:
#     type:
#       type: string
#       example: teacher-badge
#       description: teacher-badge
#       readOnly: true

SimpleModel_teacher-badge_id:
  type: integer
  format: int32
  example: 1
  readOnly: true
  description: Unique identifier of the object
