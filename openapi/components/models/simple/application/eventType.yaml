SimpleModel_application-eventType:
  type: object
  allOf:
    - $ref: "#/SimpleModel_application-eventType_id"

    # works
    - properties:
        name:
          $ref: "../../library/application/eventType.yaml#/application-eventType_name"

    # doesn't work
    # - $ref: "../../library/application/eventType.yaml#/application-eventType_properties_name"

    - $ref: "../../library/application/eventType.yaml#/application-eventType_relationshipIds"

# shared
SimpleModel_application-eventType_id:
  properties:
    id:
      type: integer
      format: int32
      example: 1
      readOnly: true
      description: Unique identifier of the object
