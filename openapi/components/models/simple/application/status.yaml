SimpleModel_application-status:
  allOf:
    # - $ref: "#/SimpleModel_application-status"
    - type: object
      allOf:
        - $ref: "#/SimpleModel_application-status_id"
        - $ref: "../../library/application/status.yaml#/application-status_properties_name"

# shared
SimpleModel_application-status_id:
  properties:
    id:
      type: integer
      format: int32
      example: 1
      readOnly: true
      description: Unique identifier of the object
