SimpleModel_subject-subjectMonths:
  type: object
  additionalProperties: false
  properties:
    id:
      $ref: "#/SimpleModel_subject-subjectMonths_id"
    # simple attributes
    numberOfLiveJobs:
      $ref: "../../library/subject/subjectMonths.yaml#/subject-subjectMonths_numberOfLiveJobs"

    # relationship attributes
    monthId:
      $ref: "../../library/subject/subjectMonths.yaml#/subject-subjectMonths_monthId"
    subjectId:
      $ref: "../../library/subject/subjectMonths.yaml#/subject-subjectMonths_subjectId"    
  required:
    - id
    - numberOfLiveJobs
    - monthId
    - subjectId

# shared
SimpleModel_subject-subjectMonths_id:
  type: integer
  example: 1
  readOnly: true
  description: Unique identifier of the object