SimpleModel_subject-subjectMonth:
  type: object
  additionalProperties: false
  properties:
    id:
      $ref: "#/SimpleModel_subject-subjectMonth_id"
    # simple attributes
    numberOfLiveJobs:
      $ref: "../../library/subject/subjectMonth.yaml#/subject-subjectMonth_numberOfLiveJobs"

    # relationship attributes
    monthId:
      $ref: "../../library/subject/subjectMonth.yaml#/subject-subjectMonth_monthId"
    subjectId:
      $ref: "../../library/subject/subjectMonth.yaml#/subject-subjectMonth_subjectId"    
  required:
    - id
    - numberOfLiveJobs
    - monthId
    - subjectId

# shared
SimpleModel_subject-subjectMonth_id:
  type: integer
  example: 1
  readOnly: true
  description: Unique identifier of the object