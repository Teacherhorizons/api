SimpleModel_job:
  allOf:
    # - $ref: "#/SimpleModel_job"
    - type: object
      allOf:
        - $ref: "#/SimpleModel_job_id"
        - $ref: "../../library/job/job.yaml#/job_attributes"
        - $ref: "../../library/job/job.yaml#/job_relationshipIds"

# shared
SimpleModel_job_id:
  properties:
    id:
      type: integer
      format: int32
      example: 1
      readOnly: true
      description: Unique identifier of the object
