SimpleModel_job-status:
  allOf:
    - $ref: "#/SimpleModel_job-status"
    - type: object
      allOf:
        - $ref: "#/SimpleModel_job-status_type"
        - $ref: "#/SimpleModel_job-status_id"
        - $ref: "../../library/job/status.yaml#/job-status_properties_name"

# shared

SimpleModel_job-status_type:
  properties:
    type:
      type: string
      example: job-status
      description: job-status
      readOnly: true

SimpleModel_job-status_id:
  properties:
    id:
      type: integer
      format: int32
      example: 1
      readOnly: true
      description: Unique identifier of the object

