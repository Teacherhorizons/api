SimpleModel_time-month:
  type: object
  additionalProperties: false
  properties:
    id:
      $ref: "#/SimpleModel_time-month_id"
    # simple attributes
    dateId:
      $ref: "../../library/time/month.yaml#/time-month_dateId"
    monthNumber:
      $ref: "../../library/time/month.yaml#/time-month_monthNumber"
  required:
    - id
    - dateId
    - monthNumber

# shared
SimpleModel_time-month_id:
  type: integer
  example: 1
  readOnly: true
  description: Unique identifier of the object