key:
  name: key
  in: query
  description: Comma separated list of list keys (keywords); TODO - consider renaming to 'keys'
  example: subjects,countries
  schema:
    type: string

search:
  name: search
  in: query
  description: Term to search
  example: Foo bar
  schema:
    type: string
