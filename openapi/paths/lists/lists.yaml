get:
  tags:
    - lists
  summary: Get lists matching the key (list of names)
  parameters:
    - $ref: '../../components/parameters/general.yaml#/key'
  responses:
    '200':
      description: successful operation
      content:
        application/json:
          schema:
            - $ref: '../../components/schemas/Lists.yaml'
