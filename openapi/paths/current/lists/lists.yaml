get:
  tags:
    - Lists
    - status-current
  summary: Get lists matching the key (current)
  parameters:
    - $ref: "../../../components/parameters/current/general.yaml#/key"
  responses:
    "200":
      description: successful operation
      content:
        application/json:
          schema:
            $ref: "../../../components/schemas/lists/Lists.yaml"
