get:
  tags:
    - Academy
    - status-current
  summary: Get Academy details (current)
  parameters:
    - in: path
      name: accreditationRating
      description: Get accreditation rating
      example: 5
      schema:
        type: integer
        format: int32
  responses:
    "200":
      description: successful operation
      content:
        application/json:
          schema:
            $ref: "../../../components/schemas/current/academy/academy-response.yaml"
    "400":
      description: Invalid ID supplied
    "404":
      description: Pet not found
  security:
    - api_key: []
