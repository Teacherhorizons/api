get:
  operationId: getRegions_schoolCounts_current
  tags:
    - schools
    - status-current
  summary: Get school count by region (current)
 # parameters:

  responses:
    "200":
      description: successful operation
      content:
        application/json:
          schema:
            $ref: "../../../components/schemas/current/schools/schools-search-region-area-by-region-count.yaml"
    "400":
      description: Invalid ID supplied
    "404":
      description: Pet not found
  security:
    - api_key: []
