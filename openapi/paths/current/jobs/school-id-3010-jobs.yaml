get:
  operationId: getSchooljob_current
  tags:
    - jobs
    - status-current
  summary: Get list of jobs for a school (current)
  #parameters:

  responses:
    "200":
      description: successful operation
      content:
        application/json:
          schema:
            $ref: "../../../components/schemas/current/jobs/school-id-3010-jobs.yaml"
    "400":
      description: Invalid ID supplied
    "404":
      description: Pet not found
  security:
    - api_key: []