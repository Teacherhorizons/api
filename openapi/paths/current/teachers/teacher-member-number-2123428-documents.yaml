get:
  operationId: getTeacher_documents_current
  tags:
    - teachers
    - status-current
  summary: Get teacher documents (current)
  #parameters
  responses:
    "200":
      description: successful operation
      content:
        application/json:
          schema:
            $ref: "../../../components/schemas/current/teachers/teacher-member-number-2123428-documents.yaml"
    "400":
      description: Invalid ID supplied
    "404":
      description: Pet not found
  security:
    - api_key: []