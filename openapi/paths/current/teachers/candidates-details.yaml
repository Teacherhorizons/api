get:
  operationId: getTeacherDetails_current
  tags:
    - teachers
    - status-current
  summary: Get details of a Teacher (current)
 # parameters:
  responses:
    "200":
      description: successful operation
      content:
        application/json:
          schema:
            $ref: "../../../components/schemas/current/teachers/candidates-details.yaml"
    "400":
      description: Invalid ID supplied
    "404":
      description: Pet not found
  security:
    - api_key: []