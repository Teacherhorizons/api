get:
  operationId: get-regional-regions
  tags:
    - regional
  summary: Get regions
  parameters:
    - in: query
      name: schema
      required: true
      schema:
        type: string
        enum:
          - admin
          - school
          - teacher
    - in: query
      name: fields
      style: deepObject
      allowReserved: true
      explode: true
      schema:
        type: object
        properties:
          # simple attributes
          name:
            $ref: "../../components/models/library/regional/region.yaml#/regional-region_alias"
        example:
          region: name,slug,locationLatLong
    - in: query
      name: filter
      style: deepObject
      allowReserved: true
      explode: true
      schema:
        type: object
        properties:
          # simple attributes
          name:
            $ref: "../../components/models/library/regional/region.yaml#/regional-region_alias"
        example:
          name: foo
          slug: bar
  responses:
    "200":
      description: OK (Request successful)
      content:
        application/json:
          schema:
            $ref: "../../components/responses/regional/regions.yaml#/Response_regional-regions"
    "400":
      $ref: "../../components/models/json-api/standard/error.yaml#/JsonApiModel_error_400"
    "404":
      $ref: "../../components/models/json-api/standard/error.yaml#/JsonApiModel_error_404"

